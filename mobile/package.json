{
  "name": "ubetra-android",
  "version": "0.79.0",
  "private": true,
  "description": "UBETRA Capacitor Android shell — loads the HTTPS PWA with native FCM for reliable push and future calls",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/app": "^7.0.1",
    "@capacitor/core": "^7.4.2",
    "@capacitor/push-notifications": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
