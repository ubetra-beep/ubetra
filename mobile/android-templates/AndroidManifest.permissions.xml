<!-- Merge these into android/app/src/main/AndroidManifest.xml after `npx cap add android`. -->
<manifest>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <!-- Required to let the Calls channel bypass Do Not Disturb (user must grant in system settings). -->
  <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />

  <application>
    <!-- Replace BridgeActivity with .MainActivity in the launcher activity android:name -->
  </application>
</manifest>
